CREATE TABLE students
(
    id BIGINT PRIMARY KEY NOT NULL,
    studentid int NOT NULL,
    name varchar(50),
    sex varchar(10),
    phone int,
    college varchar(30),
    class varchar(30),
    photo varchar(255)
);